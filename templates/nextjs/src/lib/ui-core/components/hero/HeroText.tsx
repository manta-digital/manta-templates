import React from 'react';
import { HeroTextProps } from '../../types/hero';

/**
 * Hero text component
 *
 * Renders title, subtitle, and description with proper
 * heading hierarchy and animation support.
 */
export function HeroText(props: HeroTextProps) {
  // TODO: Implement in Task 2.3
  return (
    <div className="hero-text">
      <h1>Hero Text Component - Placeholder</h1>
    </div>
  );
}

HeroText.displayName = 'HeroText';