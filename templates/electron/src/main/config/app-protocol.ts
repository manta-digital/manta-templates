/**
 * App Protocol Configuration
 *
 * IMPORTANT: Change this to a unique value for each deployed app to avoid
 * conflicts with other Electron apps on the same system.
 *
 * This protocol is used to load the app's UI files (HTML, CSS, JS).
 * Multiple apps using the same protocol will conflict.
 *
 * Examples:
 * - Template: 'electron-template-app'
 * - Your app: 'myapp' or 'company-appname'
 * - Context Forge: 'context-forge'
 *
 * The protocol will be used as: `${APP_PROTOCOL_SCHEME}://./index.html`
 */
export const APP_PROTOCOL_SCHEME = 'electron-template-app'
