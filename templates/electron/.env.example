# Auth0 Configuration
# Set AUTH_ENABLED=true to enable authentication (default: disabled)
AUTH_ENABLED=false

# Get these values from your Auth0 dashboard: https://manage.auth0.com/
# Create a Native Application type for this Electron app
#
# IMPORTANT: In Auth0 dashboard "Allowed Callback URLs", add:
#   http://localhost:52847/callback
#
# Note: Auth0 and Okta don't support RFC 8252 dynamic ports (despite the OAuth spec
# requiring it for native apps), so we use a fixed high port to minimize conflicts.
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your_native_app_client_id
AUTH0_AUDIENCE=https://your-api.com  # Optional - only needed if calling a custom API
